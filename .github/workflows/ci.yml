# This file is superseded by:
#   workflows/qa.yml   — full QA suite (Jest + Pytest + Playwright + Health Score)
#   workflows/lint.yml — ESLint + TypeScript type check
#
# Both run on every push and pull_request automatically.
# This file is kept for reference only and is disabled.
